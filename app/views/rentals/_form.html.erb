<%= simple_form_for(@rentals, method: "post") do |f| %>
  <%= f.input :number_box, :label => "Nombre de (max : #{@rentals.max_capacity} boîtes)", input_html: { max: @rentals.max_capacity, min: 1, value: 1} %>
  <%= f.input :rental_time_start, :label => "Date de récupération", placeholder: "Date de rendu", as: :string, required: false, input_html: {class: "datepicker"} %>
  <%= f.input :rental_time_end, :label => "Date de rendu", placeholder: "Date de rendu", as: :string, required: false, input_html: {class: "datepicker"} %>
  <%= f.submit "Je réserve", class: "btn btn-secondary btn-lg btn-block reservation resa-form" %>
<% end %>
